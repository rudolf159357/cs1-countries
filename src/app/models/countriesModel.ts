export interface Countries {
    id: number,
    name: string,
    capitalCity: string,
    currency: string,
    totalArea: number,
    language: string,
    population: number,
    cityPopulation: number
}